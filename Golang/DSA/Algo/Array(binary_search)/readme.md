## Complexity Breakdown

### Time Complexity: 
$O(\log n)$ because the search space is cut in half with each iteration or recursive step, requiring at most $\log_2(n)$ operations for n elements.

### Space Complexity: 
O(1) constant space for the iterative loop, or $O(\log n)$ space if using a recursive approach due to the call stack.